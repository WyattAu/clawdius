name: Lean Action CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Lean (elan)
        uses: jaxx0/setup-lean@v1

      - name: Build Lean4 proofs
        run: lake build
