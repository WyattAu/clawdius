apiVersion: v1
kind: Service
metadata:
  name: {{ include "clawdius.fullname" . }}
  labels:
    {{- include "clawdius.labels" . | nindent 4 }}
spec:
  type: {{ .Values.gateway.service.type }}
  ports:
    - port: {{ .Values.gateway.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clawdius.selectorLabels" . | nindent 4 }}
